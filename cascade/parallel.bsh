
# order 6
k=6
for i in {1..14}
do
  mkdir -p working_${k}_${i}
  cd working_${k}_${i}
  ../../../bin/mace4 -n6 -N6 -A1 -m-1 -f ../inputs/monoid_${i}.in >> ${k}_${i}.log &
  pids[${i}]=$!
  cd ..
done

# wait for all pids
for pid in ${pids[*]}; do
    wait $pid
done
